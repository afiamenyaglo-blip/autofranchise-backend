# ── Serveur ────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development

# ── Base de données MySQL ───────────────────────────────────────────
# Développement local (XAMPP, WAMP, ou MySQL installé)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=autofranchise

# ── OU utiliser une URL complète (Render / Railway / PlanetScale) ───
# DATABASE_URL=mysql://user:password@host:3306/autofranchise

# ── Sécurité JWT ────────────────────────────────────────────────────
JWT_SECRET=autofranchise_togo_super_secret_2025_changez_moi
JWT_EXPIRES_IN=7d

# ── Upload images ───────────────────────────────────────────────────
UPLOAD_DIR=./uploads

# ── Frontend ────────────────────────────────────────────────────────
FRONTEND_URL=http://localhost:3000

# ── Mobile Money (Production) ───────────────────────────────────────
FLOOZ_API_KEY=votre_cle_moov_togo
FLOOZ_API_URL=https://api.moov-africa.tg/v1
TMONEY_API_KEY=votre_cle_togocom
TMONEY_API_URL=https://api.togocom.tg/tmoney/v1
